---
icon: ph:book-open-duotone
---

# Getting Started

> Build web-standard servers with context-based handlers, middleware, and runtime adapters.

## Install

:pm-install{name="servok"}

## Quick Start

Create a server entry:

```ts [server.ts]
import { serve } from "servok";
import { NodeRuntimeAdapter } from "servok/node";

serve({
  adapter: new NodeRuntimeAdapter(),
  fetch() {
    return Response.json({ hello: "world" });
  },
});
```

Run it with Node.js:

```bash
node --experimental-strip-types server.ts
```

## Quick Start with CLI

Create a default entry file:

```ts [server.ts]
export default {
  fetch() {
    return new Response("Hello from servok");
  },
};
```

Then run:

:pm-x{command="servok"}

Or after installation, run the installed binary directly:

```bash
servok
```

The CLI will look for common entry names such as:

- `server.ts`
- `server.mjs`
- `src/server.ts`
- `src/server.mjs`

If your entry uses another name or extension, pass it explicitly:

```bash
servok --entry ./server.tsx --import jiti/register
```

:read-more{to="/guide/cli" title="Using the CLI"}

## Runtime Adapters

`servok` keeps request handling runtime-agnostic. You provide an adapter for the
host environment you want to run on.

- `servok/node`
- `servok/bun`
- `servok/deno`
- `servok/stream`

## Main Exports

```ts
import {
  Server,
  ServerServeEvent,
  ServerCloseEvent,
  ServerErrorEvent,
  serve,
  createContextKey,
} from "servok";
import { log } from "servok/log";
import { serveStatic } from "servok/static";
import { StreamRuntimeAdapter } from "servok/stream";
```

## Examples

The repository includes runnable examples under [`examples/`](https://github.com/hornjs/servok/tree/main/examples):

- `examples/basic/node.ts`
- `examples/basic/bun.ts`
- `examples/basic/deno.ts`
- `examples/basic/stream.ts`
- `examples/jsx/server.tsx`
